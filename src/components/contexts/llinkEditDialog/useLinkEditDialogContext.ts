import { linkEditDialogContext } from '@/components/contexts/llinkEditDialog/linkEditDialogContext';
import React from 'react';

export const useLinkEditDialogContext = () => React.useContext(linkEditDialogContext);
